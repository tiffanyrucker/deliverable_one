import java.util.Scanner;

public class AddDigits{

 	public static boolean validateInput(int number1, int number2) //this method takes two numbers as input and returns true if the sum of the digits is the same else returns false

 	 {
		boolean result = false; 	// initialize variable false initially 
		if(number1 != 0 || number 2 != 0 ) // except condition if one of the number is 0 because our logic to check length of number fail on input 0 because we cannot take 0 
			{
			int length1 = (int) (Math.log10(number1) + 1); 
			int length2 = (int) (Math.log10(number2) + 1);
			

				if (length1 == length2) { // if lengths are the same

			String num1 = String.valueOf (number1); 
			String num2 = String.valueOf (number2);
				result = true;	// if the lengths are the same initially set result to true
			int sum =Character.digit(num1.charAt(0), 10) + Character.digit (num2.charAt (0), 10; // Get first character from num1 and num2 string then convert them to integers and add them 


			for (int i = 0; i < num1.length(); i++) { 
			int x = Character.digit (num1.charAt(i), 10); 
			int y = Character.digit(num2.charAt(i), 10);

			if ((x+y) != sum) { // if the sum of the two digits is not the same of the sum of first digits
			
			result = false; // set result to false
		}
	}






			int y = Character.digit(num2.charAt(i), 10);